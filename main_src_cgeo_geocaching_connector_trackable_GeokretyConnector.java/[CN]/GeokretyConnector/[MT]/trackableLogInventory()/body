{
  return Observable.from(loadInventory()).map(new Func1<Trackable,TrackableLog>(){
    @Override public TrackableLog call(    final Trackable trackable){
      return new TrackableLog(trackable.getGeocode(),trackable.getTrackingcode(),trackable.getName(),getId(trackable.getGeocode()),0,trackable.getBrand());
    }
  }
);
}
