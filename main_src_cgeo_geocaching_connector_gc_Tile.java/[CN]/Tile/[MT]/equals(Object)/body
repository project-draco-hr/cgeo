{
  if (this == o) {
    return true;
  }
  if (!(o instanceof Tile)) {
    return false;
  }
  final Tile other=(Tile)o;
  return this.tileX == other.tileX && this.tileY == other.tileY && this.zoomLevel == other.zoomLevel;
}
