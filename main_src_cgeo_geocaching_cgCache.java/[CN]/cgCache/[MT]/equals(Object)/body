{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  return geocode != null ? geocode.compareTo(((cgCache)obj).geocode) == 0 : false;
}
