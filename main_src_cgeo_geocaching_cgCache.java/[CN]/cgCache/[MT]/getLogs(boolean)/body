{
  if (allLogs) {
    return logs.get();
  }
  ArrayList<LogEntry> friendLogs=new ArrayList<LogEntry>();
  for (  LogEntry log : logs.get()) {
    if (log.friend) {
      friendLogs.add(log);
    }
  }
  return friendLogs;
}
