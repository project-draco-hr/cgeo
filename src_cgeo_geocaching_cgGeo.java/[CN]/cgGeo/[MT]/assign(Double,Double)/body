{
  if (lat == null || lon == null) {
    return;
  }
  gps=-1;
  latitudeNow=lat;
  longitudeNow=lon;
  altitudeNow=null;
  bearingNow=Double.valueOf(0);
  speedNow=0f;
  accuracyNow=999f;
  if (geoUpdate != null) {
    geoUpdate.updateLoc(this);
  }
}
