{
  if (coords == null) {
    return;
  }
  gps=-1;
  coordsNow=coords;
  altitudeNow=null;
  bearingNow=0f;
  speedNow=0f;
  accuracyNow=999f;
  if (geoUpdate != null) {
    geoUpdate.updateLoc(this);
  }
}
