{
  if (coords == null) {
    return;
  }
  gps=-1;
  coordsNow=coords;
  altitudeNow=null;
  bearingNow=Double.valueOf(0);
  speedNow=0f;
  accuracyNow=999f;
  if (geoUpdate != null) {
    geoUpdate.updateLoc(this);
  }
}
