{
  final JSONObject response=Network.requestJSON("http://status.cgeo.org/api/status.json",new Parameters("version_code","" + Version.getVersionCode(),"version_name",Version.getVersionName(),"locale",Locale.getDefault().toString()));
  if (response != null) {
    notifyObservers(new Status(get(response,"message"),get(response,"message_id"),get(response,"icon"),get(response,"url")));
  }
}
