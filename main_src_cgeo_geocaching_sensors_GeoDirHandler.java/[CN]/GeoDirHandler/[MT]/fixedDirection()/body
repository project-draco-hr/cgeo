{
  return app.directionObservable().map(new Func1<Float,Float>(){
    @Override public Float call(    final Float direction){
      final GeoData geoData=app.currentGeo();
      return fixDirection(geoData,direction);
    }
  }
);
}
