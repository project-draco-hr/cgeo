{
  final int mfSourceId1=getMapsforgeSource(sourceId1);
  final int mfSourceId2=getMapsforgeSource(sourceId2);
  return mfSourceId1 == mfSourceId2 || !isMapfile024(Settings.getMapFile()) || mfSourceId1 != OFFLINE && mfSourceId2 != OFFLINE;
}
