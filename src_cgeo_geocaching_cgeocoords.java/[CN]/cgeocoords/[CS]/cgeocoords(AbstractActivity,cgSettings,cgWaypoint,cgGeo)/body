{
  super(contextIn);
  context=contextIn;
  settings=settingsIn;
  geo=geoIn;
  if (waypoint != null) {
    coords=waypoint.coords;
  }
 else   if (geo != null && geo.coordsNow != null) {
    coords=geo.coordsNow;
  }
}
