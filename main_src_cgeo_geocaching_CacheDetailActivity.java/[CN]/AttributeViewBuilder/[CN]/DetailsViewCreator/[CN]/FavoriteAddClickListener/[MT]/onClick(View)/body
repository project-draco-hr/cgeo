{
  doExecute(R.string.cache_dialog_favorite_add_title,R.string.cache_dialog_favorite_add_message,new Action1<SimpleCancellableHandler>(){
    @Override public void call(    final SimpleCancellableHandler simpleCancellableHandler){
      favoriteAdd(simpleCancellableHandler);
    }
  }
);
}
