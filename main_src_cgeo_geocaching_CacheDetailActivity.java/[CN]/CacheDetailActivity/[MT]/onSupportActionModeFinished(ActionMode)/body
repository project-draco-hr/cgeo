{
  mActionMode=null;
  if (selectedTextView != null)   selectedTextView.setWindowFocusWait(false);
  if (!mSelectionModeActive) {
    selectedTextView=null;
  }
  super.onSupportActionModeFinished(mode);
}
