{
  Cursor cursor=database.rawQuery("SELECT DISTINCT " + column + " FROM "+ table+ " WHERE "+ column+ " LIKE ?"+ " ORDER BY "+ column+ " COLLATE NOCASE ASC;",new String[]{getSuggestionArgument(input)});
  return getFirstColumn(cursor);
}
