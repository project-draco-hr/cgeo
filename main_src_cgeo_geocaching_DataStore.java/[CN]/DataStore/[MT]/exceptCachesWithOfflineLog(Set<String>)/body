{
  if (geocodes.isEmpty()) {
    return geocodes;
  }
  init();
  final Cursor cursor=database.query(dbTableLogsOffline,new String[]{"geocode"},null,null,null,null,null);
  final List<String> geocodesWithOfflineLog=Arrays.asList(getFirstColumn(cursor));
  geocodes.removeAll(geocodesWithOfflineLog);
  return geocodes;
}
