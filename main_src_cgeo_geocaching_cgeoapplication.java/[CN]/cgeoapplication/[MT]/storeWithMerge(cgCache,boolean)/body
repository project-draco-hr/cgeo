{
  if (!override) {
    final cgCache oldCache=storage.loadCache(cache.getGeocode(),cache.getGuid(),true,true,true,true,true,true);
    cache.gatherMissingFrom(oldCache);
  }
  return storage.saveCache(cache);
}
