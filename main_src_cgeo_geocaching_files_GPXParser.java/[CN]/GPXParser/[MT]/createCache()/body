{
  final Geocache newCache=new Geocache();
  newCache.setReliableLatLon(true);
  newCache.setAttributes(Collections.<String>emptyList());
  newCache.setWaypoints(Collections.<Waypoint>emptyList(),false);
  return newCache;
}
