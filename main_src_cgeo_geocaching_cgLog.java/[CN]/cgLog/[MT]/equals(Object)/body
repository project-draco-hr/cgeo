{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof cgLog)) {
    return false;
  }
  final cgLog otherLog=(cgLog)obj;
  return date == otherLog.date && type == otherLog.type && author.compareTo(otherLog.author) == 0 && log.compareTo(otherLog.log) == 0 ? true : false;
}
