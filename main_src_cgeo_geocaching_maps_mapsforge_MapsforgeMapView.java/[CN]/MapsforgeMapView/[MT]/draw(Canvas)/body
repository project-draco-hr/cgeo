{
  try {
    if (getMapZoomLevel() >= 22) {
      getController().setZoom(22);
    }
    super.draw(canvas);
  }
 catch (  Exception e) {
    Log.e(Settings.tag,"cgMapView.draw: " + e.toString());
  }
}
