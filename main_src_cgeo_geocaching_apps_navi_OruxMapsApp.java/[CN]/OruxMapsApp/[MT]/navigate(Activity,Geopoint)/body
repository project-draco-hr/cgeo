{
  final Intent intent=new Intent(INTENT);
  intent.putExtra(ORUXMAPS_EXTRA_LATITUDE,point.getLatitude());
  intent.putExtra(ORUXMAPS_EXTRA_LONGITUDE,point.getLongitude());
  activity.startActivity(intent);
}
