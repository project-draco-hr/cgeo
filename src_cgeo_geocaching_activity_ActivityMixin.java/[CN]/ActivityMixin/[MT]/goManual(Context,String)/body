{
  if (helpTopic == null || helpTopic.length() == 0) {
    return;
  }
  try {
    AppManualReaderClient.openManual("c-geo",helpTopic,context,"http://cgeo.carnero.cc/manual/");
  }
 catch (  Exception e) {
  }
}
