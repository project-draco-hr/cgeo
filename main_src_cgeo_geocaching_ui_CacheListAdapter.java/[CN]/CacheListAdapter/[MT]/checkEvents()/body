{
  eventsOnly=true;
  for (  final Geocache cache : list) {
    if (!cache.isEventCache()) {
      eventsOnly=false;
      if (isSortedByEvent()) {
        setComparator(DistanceComparator.singleton);
      }
      return;
    }
  }
}
