{
  if (isInvalidGeocode(geocode)) {
    return GC_CONNECTOR;
  }
  for (  IConnector connector : connectors) {
    if (connector.canHandle(geocode)) {
      return connector;
    }
  }
  return GC_CONNECTOR;
}
