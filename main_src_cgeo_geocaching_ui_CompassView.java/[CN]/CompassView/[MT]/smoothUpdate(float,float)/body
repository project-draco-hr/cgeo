{
  final float diff=AngleUtils.difference(actual,goal);
  float offset=0;
  if (diff > 1.0) {
    offset=(float)Math.ceil(diff / 10.0);
  }
 else   if (diff < 1.0) {
    offset=(float)Math.floor(diff / 10.0);
  }
  return AngleUtils.normalize(actual + offset);
}
