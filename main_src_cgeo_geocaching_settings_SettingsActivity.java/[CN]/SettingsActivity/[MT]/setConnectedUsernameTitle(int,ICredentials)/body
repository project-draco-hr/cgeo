{
  final Credentials credentials=Settings.getCredentials(connector);
  getPreference(prefKeyId).setSummary(StringUtils.isNotBlank(credentials.getUsernameRaw()) ? getString(R.string.auth_connected_as,credentials.getUserName()) : getString(R.string.auth_unconnected));
}
