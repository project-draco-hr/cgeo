{
  if ((cache1.coords == null || cache2.coords == null) && cache1.distance != null && cache2.distance != null) {
    return Double.compare(cache1.distance,cache2.distance);
  }
 else {
    if (cache1.coords == null) {
      return 1;
    }
    if (cache2.coords == null) {
      return -1;
    }
    return Float.compare(coords.distanceTo(cache1.coords),coords.distanceTo(cache2.coords));
  }
}
