{
  final List<Image> finalLogImage=logImages == null ? Collections.<Image>emptyList() : logImages;
  return new LogEntry(id,logType,StringUtils.defaultIfBlank(author,Settings.getUserName()),message,date,found,friend,finalLogImage,cacheName,cacheGuid,cacheGeocode);
}
