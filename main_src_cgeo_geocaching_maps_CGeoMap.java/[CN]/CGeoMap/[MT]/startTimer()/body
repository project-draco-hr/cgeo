{
  if (coordsIntent != null) {
    (new DisplayPointThread()).start();
  }
 else {
    if (loadTimer != null) {
      loadTimer.stopIt();
      loadTimer=null;
    }
    loadTimer=new LoadTimer();
    loadTimer.start();
  }
}
