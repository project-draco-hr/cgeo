{
  if (dir == null || dir.directionNow == null) {
    return;
  }
  if (overlayPosition != null && mapView != null && (app.currentGeo().getSpeedNow() <= 5)) {
    overlayPosition.setHeading(dir.directionNow);
    mapView.repaintRequired(overlayPosition);
  }
}
