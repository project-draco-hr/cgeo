{
  if (!Settings.isLiveList()) {
    return;
  }
  if (dir == null || dir.directionNow == null) {
    return;
  }
  northHeading=dir.directionNow;
  if (northHeading != null && adapter != null && (geo == null || geo.speedNow <= 5)) {
    adapter.setActualHeading(northHeading);
  }
}
