{
  startGeoAndDir();
  if (CollectionUtils.isNotEmpty(cacheList)) {
    setMoreCaches();
  }
  setTitle(title);
  setAdapter();
  if (geo != null) {
    geoUpdate.updateLocation(geo);
  }
  if (dir != null) {
    dirUpdate.updateDirection(dir);
  }
}
