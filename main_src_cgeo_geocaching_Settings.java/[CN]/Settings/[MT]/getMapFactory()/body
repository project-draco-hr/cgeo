{
  if (mapFactory == null) {
    if (getMapSource().isGoogleMapSource()) {
      mapFactory=new GoogleMapFactory();
    }
 else {
      mapFactory=new MapsforgeMapFactory();
    }
  }
  return mapFactory;
}
