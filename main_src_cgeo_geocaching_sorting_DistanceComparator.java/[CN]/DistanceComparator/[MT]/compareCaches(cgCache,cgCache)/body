{
  if ((cache1.getCoords() == null || cache2.getCoords() == null) && cache1.getDistance() != null && cache2.getDistance() != null) {
    return Double.compare(cache1.getDistance(),cache2.getDistance());
  }
 else {
    if (cache1.getCoords() == null) {
      return 1;
    }
    if (cache2.getCoords() == null) {
      return -1;
    }
    return Float.compare(coords.distanceTo(cache1.getCoords()),coords.distanceTo(cache2.getCoords()));
  }
}
