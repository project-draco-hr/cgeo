{
  List<Tile> tiles=new ArrayList<Tile>();
  if (!Settings.isPremiumMember()) {
    tiles.add(new Tile(viewport.getCenter(),14));
  }
  tiles.add(new Tile(viewport.getCenter(),12));
  return tiles;
}
