{
switch (msg.what) {
case START:
    removeMessages(ACT);
  sendEmptyMessage(ACT);
break;
case STOP:
removeMessages(ACT);
break;
case ACT:
sendEmptyMessageDelayed(ACT,period);
act();
break;
default :
throw new UnsupportedOperationException();
}
}
