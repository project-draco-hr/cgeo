{
  if (obj instanceof UTMPoint) {
    final UTMPoint pnt=(UTMPoint)obj;
    return northing == pnt.northing && easting == pnt.easting && zoneNumber == pnt.zoneNumber && zoneLetter == pnt.zoneLetter;
  }
  return false;
}
