{
  File dir=new File(cgSettings.getStorage());
  if (dir.exists() == false) {
    dir.mkdirs();
  }
  dir=new File(getStaticMapsDirectory(cache));
  if (dir.exists() == false) {
    dir.mkdirs();
  }
}
