{
  File dir=new File(Settings.getStorage());
  if (dir.exists() == false) {
    dir.mkdirs();
  }
  dir=new File(getStaticMapsDirectory(cache));
  if (dir.exists() == false) {
    dir.mkdirs();
  }
}
