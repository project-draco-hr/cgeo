{
  loading=true;
  if (search == null) {
    search=runSearch();
  }
 else {
    search=GCParser.searchByNextPage(new SearchResult(search),Settings.isShowCaptcha(),this);
  }
  loading=false;
  return search;
}
