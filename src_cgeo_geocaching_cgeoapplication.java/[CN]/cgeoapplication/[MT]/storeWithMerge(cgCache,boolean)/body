{
  if (!override) {
    final cgCache oldCache=storage.loadCache(cache.geocode,cache.guid,true,true,true,true,true,true);
    cache.gatherMissingFrom(oldCache);
  }
  return storage.saveCache(cache);
}
