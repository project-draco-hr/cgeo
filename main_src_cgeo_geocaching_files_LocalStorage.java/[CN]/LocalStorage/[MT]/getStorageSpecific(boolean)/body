{
  return Environment.getExternalStorageState().equals(Environment.MEDIA_MOUNTED) ^ secondary ? new File(Environment.getExternalStorageDirectory(),Settings.cache) : new File(new File(new File(Environment.getDataDirectory(),"data"),"cgeo.geocaching"),Settings.cache);
}
