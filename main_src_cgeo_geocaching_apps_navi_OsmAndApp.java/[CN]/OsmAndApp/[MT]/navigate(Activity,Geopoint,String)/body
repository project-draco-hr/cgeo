{
  final Parameters params=new Parameters(PARAM_LAT,String.valueOf(coords.getLatitude()),PARAM_LON,String.valueOf(coords.getLongitude()),PARAM_NAME,markerName);
  activity.startActivity(buildIntent(params));
}
