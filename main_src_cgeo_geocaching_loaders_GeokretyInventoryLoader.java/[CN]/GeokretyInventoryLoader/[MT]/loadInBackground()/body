{
  try {
    for (    final Trackable trackable : connector.loadInventory()) {
      trackables.add(new TrackableLog(trackable.getGeocode(),trackable.getTrackingcode(),trackable.getName(),GeokretyConnector.getId(trackable.getGeocode()),0,trackable.getBrand()));
    }
    return trackables;
  }
 catch (  final RuntimeException e) {
    Log.e("GeokretyInventoryLoader.loadInBackground",e);
  }
  return trackables;
}
