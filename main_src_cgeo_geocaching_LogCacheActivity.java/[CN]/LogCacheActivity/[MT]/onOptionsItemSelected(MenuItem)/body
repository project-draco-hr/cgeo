{
  if (super.onOptionsItemSelected(item)) {
    return true;
  }
  final int id=item.getItemId();
  if (id >= 10 && id <= 19) {
    rating=(id - 9) / 2.0;
    if (rating < 1) {
      rating=0;
    }
    updatePostButtonText();
    return true;
  }
  return false;
}
